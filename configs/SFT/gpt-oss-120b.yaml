model_name_or_path: axolotl-ai-co/gpt-oss-120b-dequantized
dataset_name: HuggingFaceH4/Multilingual-Thinking
# dataset_name: dnotitia/dnotitia_persona_qwen3-235b_400_v2
# dataset_name: dnotitia/persona_kai_001

max_length: 4096
packing: true

# gpt-oss does not yet support assistant_only_loss in its template
assistant_only_loss: false
num_train_epochs: 1

eos_token: 

# Step-based saves are off (save_strategy: "no" in _defaults-SFT.yaml);
# periodic_save_seconds (6h) is the only checkpoint source, so no save_steps here.
